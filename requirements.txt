# Core scientific computing
numpy>=1.26
pandas>=2.1
matplotlib>=3.8
scipy>=1.11

# Configuration
pyyaml>=6.0

# Streamlit UI (Appendix D and full simulator interface)
streamlit>=1.28

# Document generation (used by some scripts)
python-docx>=1.1.0

# PDF report export from the Streamlit app (Phase 5)
reportlab>=4.0

# Visualization helpers
seaborn>=0.13
tqdm>=4.66

# Sankey diagrams in the Streamlit UI (Phase 2 of the UX rollout)
plotly>=5.18

# Optional: Jupyter notebook support (recommended for VSCode)
jupyterlab>=4.0
ipykernel>=6.25

# Optional: testing
pytest>=7.4

# FastAPI backend (api/ — Cost Gradient REST API)
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.7
httpx>=0.27   # used by FastAPI's TestClient and by API tests

